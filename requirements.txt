# PMO Smart Agent V1 — Runtime dependencies
# Install: pip install -r requirements.txt

fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# ── PMO Runtime ────────────────────────────────────────────────────────────────
langgraph>=1.0.0
uvicorn[standard]>=0.23.0
starlette>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0
orjson>=3.9.0
langgraph-checkpoint>=2.0.0
langgraph-sdk>=0.1.0
httpx>=0.25.0

# ── NATS Collaboration Daemon ─────────────────────────────────────────────────
# Required for daemon runtime: pip install nats-py
nats-py>=2.0.0

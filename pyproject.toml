[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gov_langgraph"
version = "0.1.0"
description = "V1 Platform Core"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]
include = ["gov_langgraph*"]
